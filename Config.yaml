server:
  host: "0.0.0.0"
  port: 8080

database:
  url: "postgres://storage_user:123@localhost:5432/storage_db"

storage:
  upload_dir: "./uploads"

auth:
  jwt_secret: "CHANGE_ME_TO_LONG_RANDOM_SECRET_STRING"
  token_ttl_hours: 24
